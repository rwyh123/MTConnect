package com.example.MTConnect.config;

public enum MappingMode {
    EXPLICIT_ONLY,   // использовать только заранее заданные правила (MapRule)
    HEURISTIC_ONLY   // использовать только авто-эвристику
}
